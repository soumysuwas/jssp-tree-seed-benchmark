# Runtime dependencies for the JSSP tree-seed benchmark.
# For a fully pinned, reproducible install use `uv sync` with the committed uv.lock instead;
# this file is provided for pip users. Python 3.12 is required (see .python-version).
#
# Tested (resolved by uv.lock): numba 0.67.0, numpy 2.1.3, pandas 3.0.5, joblib 1.5.3,
# typer 0.27.1, rich 15.0.0. The numba upper bound below keeps pip from pairing an old numba
# with a numpy it cannot build against.
numpy>=1.26,<2.2
numba>=0.60,<0.68
joblib>=1.4
pandas>=2.2
typer>=0.12
rich>=13.7

# Development / tests (optional):
#   pytest>=8.0
#   ruff>=0.5
#   mypy>=1.10
